# Round 11 — 动态页真实图占位 + 群聊发送者名字外置（报告）

- 日期：2026-08-15
- 前置：Round 10 附件面板完成（round10-attach-panel-report.md）
- 循环：真实照片 → fixture 改造 → build/shoot（13 张）→ puppeteer 自验 → codebuddy 评审 → 修复 → 终审

## 1. 本轮完成项

### 1.1 动态页真实图占位（`docs/tgqq/fixtures/tab.html` + `imgs/`）
- 下载 12 张真实照片（picsum 固定 ID，600x450，山景/工作/食物等题材）到
  `docs/tgqq/fixtures/imgs/photo01-12.jpg`，替换原「渐变 + 相机图标」占位。
- `postCard()` 增加 `imgStart` 偏移参数：不同帖子展示不同照片，避免重复；
  3 图帖用 photo01-03（爬山）、单图帖 photo05（笔记本）、9 图帖 photo04-12。
- 图片 CSS：网格格 = `aspect-ratio:1/1` 方形 + `object-fit:cover`（无拉伸）；
  单图帖 = 16:9；圆角 8px；网格 gap 4px。
- 帖子排序：9 图帖置顶，使 3 种布局（9 宫格/3 图/单图）在首屏内即可评审。
- `.tq-page-content` 底部留白 32→88px（滚动到底时最后一张卡完整露出，不被底部导航遮挡）。

### 1.2 群聊发送者名字外置（`TqChatBubble.scss`）
- tweb 原生把 `.name` 渲染在气泡内部（Telegram 风格）；QQ9 的名字在气泡外上方、
  页面底色上、12px 蓝字。
- CSS 提取：首条气泡 `margin-top:1.125rem` 预留 18px，`.name` 绝对定位
  `top:-1.3125rem`、`line-height:12px`（puppeteer 校准：名字与气泡间距 4px、
  与上一条消息间距 8px，不重叠）；出站组不受影响。

## 2. 验证矩阵（puppeteer computed-style + magick 像素）

| 检测项 | 期望 | 实测 | 状态 |
|---|---|---|---|
| 9 图帖 | 3×3 方形 101px、gap 4px | 9 格全部 101×101，行/列距 105px | ✅ |
| 3 图帖 | 方形 101px | 同网格规格 | ✅ |
| 单图帖 | 16:9 | 310×174（=1.78:1） | ✅ |
| 照片真实加载 | 无 404/重复/拉伸 | 12 张全部加载（natural 600×450），magick 采样多样纹理 | ✅ |
| 卡片圆角/边框 | 14px 圆角白卡 | 像素确认 | ✅ |
| 操作行可见 | 首卡不被导航遮挡 | actions.bottom < navTop | ✅ |
| 群聊名字外置 | 12px 蓝字在气泡外页面底上 | gap 4px；像素：名字处 #1296DB，周边为页面底色非白 | ✅ |

## 3. codebuddy round11 评审（`/tmp/cb_round11_out.txt`）
- 总体接近度 ≈85%；结构/网格/圆角/真实照片全部 PASS，无 404、无重复、无变形。
- 严重项 S1（平板第 2 卡操作行被底部导航遮挡）：核实为「feed 可滚动、截图是初始视口」
  的正常表现（与 QQ9 一致），已将内容区底部留白加大至 88px 并重排帖子顺序。
- 严重项 S2（平板单图 2.05:1）：误报——puppeteer 实测 310×174=16:9，
  codebuddy 测到的是被导航遮住下半后的可见部分。
- 轻微项 M1/M2（格宽 92 vs 99、gap 3-4px）：测量噪声，puppeteer 实测全部 101px、gap 4px。
- 未覆盖项 U1（9 图帖）：已通过重排帖子让 9 图帖进入首屏。

## 4. codebuddy round11b 终审（`/tmp/cb_round11b_out.txt`）

**总体结论：动态页 9 宫格/3 图/单图全部 PASS（照片真实、网格整齐、圆角卡片正确），
群聊名字外置实现核心要求（在气泡外、页面底色上、不重叠）。**

逐项核实 codebuddy 的缺陷反馈：
| # | codebuddy 反馈 | 核实 | 处理 |
|---|---|---|---|
| 1 | 陈默名字非蓝（采样 030303） | ❌ 误报：puppeteer 逐个查 3 个 `.name` 计算色均为 rgb(18,150,219)=#1296DB；codebuddy 采样坐标落到气泡正文文字上 | 无需改 |
| 2 | 手机底部导航为蓝色 | ❌ 误报：nav 计算背景 rgb(255,255,255)；codebuddy 把第二张卡片的照片（蓝色系）误判为导航 | 无需改 |
| 3 | 名字-气泡视觉间距 ~10px > 4px | ✅ 属实（行高 12px 时字形底到气泡约 10px） | **已修**：行高 12→10px，字形填满行盒，视觉间距 ≈6px |
| 4 | 平板/手机导航底色不一致 | ❌ 误报（同 2） | 无需改 |
| 5 | 单图帖不在首屏 | 说明性提示：feed 可滚动，单图帖在滚动区下方（QQ9 常态） | 无需改 |
| 6 | 背景局部偏暖 FFDDC3 | 压缩伪影/元素边缘 | 无需改 |

## 5. 遗留 / 后续
- 长尾：附件面板/出站自头像真机验证（需登录环境）、顶栏图标描边复核、动态页点赞评论交互。

## 5. 遗留 / 后续
- 长尾：附件面板/出站自头像真机验证（需登录环境）、顶栏图标描边复核、动态页点赞评论交互。
