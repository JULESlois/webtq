# TGQQ Round 31: 布局完善 — 消息列表顶栏 / 个人抽屉 / 聊天输入面板 / 动态页

日期：2026-08-17
基准：`e666c08`（Round 30，已推送）

## 流程

1. codebuddy（hy3）评审：`/tmp/cb_r31a.md`（12 条修复清单，对照 9.1.65 真机参考 `docs/tgqq/ref/qq9-mobile3/`），覆盖：顶栏头像/昵称/状态、去掉齿轮、删除通讯录横幅、新增头像抽屉、抽屉菜单 8 项顺序、聊天输入面板重构、动态页重构、平板同步。
2. 逐条落地后终审（`/tmp/cb_r31final.md`）。**注意：本轮 codebuddy 环境无法渲染图片**（模型无视觉能力），其结论为源码推导；所有视觉结论均用 PIL 像素探针对照真机参考逐点核实，codebuddy 推测与像素事实冲突时以像素为准（例：它推测动态页第 2 行图标应为「黑→绿/粉/蓝」，经参考图实测确认绿/粉/蓝，采纳；它推测抽屉底部为「设置/夜间/扬中」三项，经 `007-profile-drawer.jpg` 底部像素确认成立，采纳）。
3. 全部截图用 `--virtual-time-budget=2500` 重拍，避免入场动画（0.28s）被截在半途导致的错位/半宽。

## 关键像素事实（探针实测，均为 9.1.65 真机参考读数）

| 项目 | 参考实测 | 处置 |
|---|---|---|
| 列表顶栏头像 | 34px 灰底人形剪影，无「我」角标 | 已改 34px 剪影头像 |
| 顶栏副标题 | 签名 + `›`（如「活着就好 ›」） | 已加 `›` 尾缀 |
| 顶栏右上角 | 仅 `+` 一个图标 | 已移除齿轮 |
| 通讯录邀请横幅 | 真机无 | 已删 DOM + fixture.css 残留样式 |
| 抽屉头部 | 左上「打卡」胶囊、右上「状态/关闭」、昵称旁「切换账号 ▾」 | 已实现（`ProfileDrawer/index.tsx` + `.module.scss`） |
| 抽屉头部蓝渐变 | 实测 `(225,237,253)` | 保持 `#e9f2ff→#dcebfc` 渐变 |
| 抽屉底部 | 一个白色胶囊栏内三项：设置/夜间/扬中（图标上、文字下） | 已实现 |
| 聊天输入面板 | 两行：白色圆角输入框 + 蓝色渐变「发送」胶囊；下方 6 工具图标行 | 已实现（`chatInput.scss`） |
| 动态页入口第 2 行图标 | 农场绿 `(51,199,125)`、购物粉 `(235,102,114)`、意见反馈蓝 `(36,150,234)`、更多蓝 `(31,150,232)` | 已改（此前误用黑色 #1D1E20） |
| 动态页天气条 | 通栏扁平 `(242,242,242)`，无圆角无渐变 | 已改 `#f2f2f2` 通栏条 |
| 动态页亲密空间 | 灰底卡 `(246,246,246)` + 粉心 `(210,144,149)` + 文字 + 箭头 | 已改 `#f6f6f6` + 粉心（去边框/去渐变） |

## 改动文件

- `src/tgqq/components/TqSidebar.scss`：顶栏头像 34px 剪影、状态 `›`、移除齿轮。
- `src/tgqq/components/ProfileDrawer/`（新增）：个人抽屉（头部工具行 打卡/状态/关闭、切换账号、8 菜单项、底部 设置/夜间/扬中 三项胶囊栏），暗色全套。
- `src/tgqq/shell/TqMobileShell.tsx`：点击侧栏头像行打开抽屉（全局 click 监听）。
- `src/tgqq/design/chatInput.scss`：两行输入面板（白色圆角输入框 + 蓝色渐变发送胶囊 + 6 工具图标行），面板背景 `--tq-chat-page`。
- `src/tgqq/pages/Dynamics/index.tsx` + `.module.scss`：入口图标 8 色按真机像素校准；天气条改扁平通栏；亲密空间改灰底粉心；暗色变体修复（CSS Modules 内 `html.night` 须写 `:global(html.night)`，已修 Channels/Contacts/Dynamics/ProfileDrawer 四处）。
- `docs/tgqq/fixtures/`：mobile/tab/tablet 三件 fixture 同步抽屉新头部/底部（scoped 类名更新为 `_13jn8_*`）、动态页条目颜色与天气/亲密空间样式、fixture.css 清理 `.tq-invite-row`；`shoot.sh` 加 `--virtual-time-budget=2500`；65 张截图重拍。
- 新增截图：`drawer-mobile.png`、`drawer-tablet.png`、`drawer-mobile-dark.png`、`drawer-tablet-dark.png`。

## 验证

- 65 张截图全部重拍（无 FAIL）。
- PIL 探针：动态页天气条 `(242,242,242)`、亲密空间卡 `(246,246,246)`、8 图标色与参考一致（黄/蓝/绿/粉蓝蓝）；抽屉头部渐变 `(225,237,253)`、暗色抽屉头部 `(36,48,74)`；聊天输入发送胶囊 `#4D9DF5` 渐变、面板 `#EDECF1`。
- DOM 校验：三件 fixture 的抽屉（打卡/切换账号/底部三项）、动态页（8 入口/天气/亲密空间）元素齐全；邀请横幅 DOM 与 CSS 均无残留。

## 说明（有意保留的偏差）

- 抽屉头像仍为「我」圆形字标（真机为大图插画，fixture 无素材，下一轮可换真实头像图）。
- 抽屉菜单图标为线框 SVG 近似（真机为彩色图标库，非本轮范围）。
- 动态页在亲密空间下方保留 Round 23 的搭子/互动标识/幸运字符/新奇物种等模块（参考图 `010-dynamics.jpg` 截断于亲密空间，未展示下方内容；下方模块为既有功能页，保留）。
- 聊天输入第 4 个工具为「文件」（真机为红包）、第 6 个为「+」（真机为展开/关闭），属交互态差异，后续轮次处理。
